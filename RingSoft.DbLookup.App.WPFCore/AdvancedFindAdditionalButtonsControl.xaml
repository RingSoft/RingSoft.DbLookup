<UserControl x:Class="RingSoft.DbLookup.App.WPFCore.AdvancedFindAdditionalButtonsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RingSoft.DbLookup.App.WPFCore"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Row="0" Grid.Column="0" x:Name="ImportDefaultLookupButton1" Content="I_mport Default Lookup"/>

        <Button Grid.Row="1" Grid.Column="0" x:Name="RefreshSettingsButton1" Content="_Refresh Settings"/>

        <Button Grid.Row="0" Grid.Column="2" Grid.RowSpan="2" x:Name="PrintLookupOutputButton1" Content="Print _Lookup Output"/>

    </Grid>
</UserControl>
