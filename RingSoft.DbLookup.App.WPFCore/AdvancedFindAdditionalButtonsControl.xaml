<UserControl x:Class="RingSoft.DbLookup.App.WPFCore.AdvancedFindAdditionalButtonsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RingSoft.DbLookup.App.WPFCore"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Row="0" Grid.Column="0" x:Name="ImportDefaultLookupButton" Content="Import Default Lookup"/>

        <Button Grid.Row="1" Grid.Column="0" x:Name="ApplyToLookupButton" Content="Apply To Lookup"/>

        <Button Grid.Row="0" Grid.Column="1" x:Name="SqlViewerButton" Content="View SQL Statement"/>

        <Button Grid.Row="1" Grid.Column="1" x:Name="RefreshSettingsButton" Content="Refresh Settings"/>

    </Grid>
</UserControl>
